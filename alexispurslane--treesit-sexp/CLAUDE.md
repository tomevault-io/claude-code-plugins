# treesit-sexp

> - use `emacs --batch --eval "(princ (documentation '<command-symbol>))"` to get documentation on a function

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/treesit-sexp/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

Tips:

- use `emacs --batch --eval "(princ (documentation '<command-symbol>))"` to get documentation on a function
- use `emacs --batch --eval "..."` in general to test expressions out. Do this often to confirm your assumptions.

---
> Source: [alexispurslane/treesit-sexp](https://github.com/alexispurslane/treesit-sexp) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-05-02 -->
