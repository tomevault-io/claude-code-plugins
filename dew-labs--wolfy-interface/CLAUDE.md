# wolfy-interface

> Using ast-grep for superior code search, syntax-aware and structural matching

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/wolfy-interface/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:


MANDATORY SEARCH TOOL: ALWAYS use `sg` (ast-grep) as FIRST OPTION for ANY code search, syntax-aware or structural matching, or even grepping task. For example: `sg -p <pattern> -l <language>`. ONLY use `rg` (ripgrep), `grep` if `sg` fails or if explicitly requested. This is OBLIGATORY - no exceptions. `sg` is way superior to plain text `rg` or `grep`.

---
> Converted and distributed by [TomeVault](https://tomevault.io/claim/dew-labs) — claim your Tome and manage your conversions.
<!-- tomevault:4.0:claude_md:2026-04-10 -->
