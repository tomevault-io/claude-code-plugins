# exograph

> - Tests mirror `lib/` under `test/exograph/` by default.

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/exograph/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

# Exograph agent notes

- Tests mirror `lib/` under `test/exograph/` by default.
- Use `test/features/` for web/API feature tests and `test/integration/` for cross-module integration or environment/compile scenarios.
- Do not add root-level `test/*_test.exs` files.

---
> Source: [elixir-vibe/exograph](https://github.com/elixir-vibe/exograph) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-09-23 -->
