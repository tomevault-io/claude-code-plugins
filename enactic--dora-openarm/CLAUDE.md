# dora-openarm

> - The first line of every pull request description must be `Fix GH-${ISSUE_NUMBER}` with `${ISSUE_NUMBER}` replaced by the matching GitHub issue number (for example: `Fix GH-123`).

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/dora-openarm/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

# Pull request description requirements

- The first line of every pull request description must be `Fix GH-${ISSUE_NUMBER}` with `${ISSUE_NUMBER}` replaced by the matching GitHub issue number (for example: `Fix GH-123`).
- Do not add a meaningless `Changes` section to the pull request description.

---
> Source: [enactic/dora-openarm](https://github.com/enactic/dora-openarm) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-08-12 -->
