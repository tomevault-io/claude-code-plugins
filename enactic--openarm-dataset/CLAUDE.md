# openarm-dataset

> - In pull request descriptions, the first line must be `Fix GH-${ISSUE_NUMBER}` with `${ISSUE_NUMBER}` replaced by the corresponding issue number (for example, `Fix GH-123`).

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/openarm-dataset/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

# GitHub Copilot instructions

- In pull request descriptions, the first line must be `Fix GH-${ISSUE_NUMBER}` with `${ISSUE_NUMBER}` replaced by the corresponding issue number (for example, `Fix GH-123`).
- Do not add a `Changes` section to pull request descriptions.

---
> Source: [enactic/openarm_dataset](https://github.com/enactic/openarm_dataset) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-08-31 -->
