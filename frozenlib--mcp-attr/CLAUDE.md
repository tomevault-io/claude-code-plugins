# rust-mcp-attr

> - `mcp_attr` に `jsoncall` の型の別名がある場合（例: `jsoncall::Error` -> `mcp_attr::Error` ）は `mcp_attr` で定義された別名を使用します。

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/rust-mcp-attr/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:


# Rustのコードの書き方

- `mcp_attr` に `jsoncall` の型の別名がある場合（例: `jsoncall::Error` -> `mcp_attr::Error` ）は `mcp_attr` で定義された別名を使用します。

---
> Source: [frozenlib/mcp-attr](https://github.com/frozenlib/mcp-attr) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-05-22 -->
