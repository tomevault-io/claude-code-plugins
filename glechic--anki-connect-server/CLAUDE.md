# anki-connect-server

> - Use `git add <files>` instead of `git add -A`

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/anki-connect-server/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

# Commit Guidelines

- Use `git add <files>` instead of `git add -A`
- Don't delete files unless explicitly asked
- Don't commit generated files
- Run quality tests before each commit (ruff, pyright, pytest)

---
> Source: [glechic/anki-connect-server](https://github.com/glechic/anki-connect-server) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-08-22 -->
