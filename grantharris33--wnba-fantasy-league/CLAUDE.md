# wnba-fantasy-league

> Always run relevant commands using poetry (e.g. `poetry run pytest -q` instead of `pytest -q`)

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/wnba-fantasy-league/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

Always run relevant commands using poetry (e.g. `poetry run pytest -q` instead of `pytest -q`)
If you are running a frontend script or command make sure to cd into the frontend directory before running the command
If a python package needs to be added, use `poetry add <package>` instead of trying to directly edit pyproject.toml

---
> Converted and distributed by [TomeVault](https://tomevault.io/claim/grantharris33) — claim your Tome and manage your conversions.
<!-- tomevault:4.0:claude_md:2026-04-09 -->
