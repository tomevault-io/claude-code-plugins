# syncdown

> - After modifying any `ts` or `json` file, always run `bun lint:fix`.

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/syncdown/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

- After modifying any `ts` or `json` file, always run `bun lint:fix`.
- After modifying any `ts` file, always run `bun typecheck`.

---
> Source: [hjinco/syncdown](https://github.com/hjinco/syncdown) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-05-05 -->
