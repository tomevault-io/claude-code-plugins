# aspire-plugin

> Most style is enforced automatically (IDE settings + `AspirePlugin.slnx.DotSettings`). The non-obvious rules:

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/aspire-plugin/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

# Code Conventions

Most style is enforced automatically (IDE settings + `AspirePlugin.slnx.DotSettings`). The non-obvious rules:

- The Kotlin compiler flag `-Xcontext-parameters` is enabled — `context(...)` declarations are valid.

---
> Source: [JetBrains/aspire-plugin](https://github.com/JetBrains/aspire-plugin) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-07-26 -->
