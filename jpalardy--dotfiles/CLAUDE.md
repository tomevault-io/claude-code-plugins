# dotfiles

> Everything in `lua/lspconfig/*` (except `util.lua`) is frozen legacy code;

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/dotfiles/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

Everything in `lua/lspconfig/*` (except `util.lua`) is frozen legacy code;
ignore that subtree when looking for bugs or analyzing patterns in the
codebase.

---
> Source: [jpalardy/dotfiles](https://github.com/jpalardy/dotfiles) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-08-16 -->
