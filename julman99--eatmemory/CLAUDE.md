# eatmemory

> - Always use braces for `if`, `else if`, and `else` bodies, even for single-line branches.

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/eatmemory/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

# Agent Instructions

- Always use braces for `if`, `else if`, and `else` bodies, even for single-line branches.
- Keep the core memory allocation function named `eat`; do not rename it to option-specific names such as `eat_with_options` or `eat_with_progress_options`.
- Keep the core memory cleanup function named `digest`; do not add option-specific digest variants.

---
> Source: [julman99/eatmemory](https://github.com/julman99/eatmemory) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-07-20 -->
