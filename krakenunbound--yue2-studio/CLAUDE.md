# yue2-studio

> The Gemini API key is stored only in `outputs/settings/api-keys.json` for local use. Never commit, force-add, upload to GitHub, or include this vault or its contents in release artifacts, logs, screenshots, or documentation. Keep the existing `.gitignore` exclusions for `outputs/` and `**/api-keys.json`.

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/yue2-studio/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

# Local credentials

The Gemini API key is stored only in `outputs/settings/api-keys.json` for local use. Never commit, force-add, upload to GitHub, or include this vault or its contents in release artifacts, logs, screenshots, or documentation. Keep the existing `.gitignore` exclusions for `outputs/` and `**/api-keys.json`.

Do not modify the source app at `F:\MiniMaxM3`.

---
> Source: [krakenunbound/yue2-studio](https://github.com/krakenunbound/yue2-studio) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-09-14 -->
