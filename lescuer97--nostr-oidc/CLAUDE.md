# nostr-oidc

> - After finishing a task try compiling the code.

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/nostr-oidc/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

## Development instructions

- After finishing a task try compiling the code. 
- Don't leave tasks half way. 
- Never commit or merge code using git. 
- Never add dependencies unless specifically authorized.

When scanning source files, use repomix to save space and tokens.
Use the Context7 MCP server to know the latest way to use libraries like `chi`, `templ`, `htmx`, `tailwindcss`.

---
> Source: [lescuer97/nostr-oidc](https://github.com/lescuer97/nostr-oidc) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-05-14 -->
