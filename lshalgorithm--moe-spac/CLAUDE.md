# css-architecture

> -   We are using Tailwind v4 which uses oklch colors so we now want to refer to the CSS vars directly, without wrapping it with any color function like `hsla/hsl`, `rgba` etc.

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/css-architecture/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:


#### Tailwind & CSS

-   We are using Tailwind v4 which uses oklch colors so we now want to refer to the CSS vars directly, without wrapping it with any color function like `hsla/hsl`, `rgba` etc.

---
> Source: [lshAlgorithm/MoE-SpAc](https://github.com/lshAlgorithm/MoE-SpAc) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-09-22 -->
