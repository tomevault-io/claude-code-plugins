# use-acp

> React hooks for the [Agent Client Protocol](https://agentclientprotocol.com/) (ACP) over WebSockets. Published to npm as `use-acp`.

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/use-acp/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

# use-acp

React hooks for the [Agent Client Protocol](https://agentclientprotocol.com/) (ACP) over WebSockets. Published to npm as `use-acp`.

## Development

```bash
pnpm install
pnpm test        # vitest
pnpm lint        # biome check --write (autofix.ci runs this on PRs)
pnpm lint:ci     # biome check, no writes
pnpm typecheck   # tsc --noEmit
pnpm build       # tsc -> dist/
```

---
> Source: [marimo-team/use-acp](https://github.com/marimo-team/use-acp) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-09-08 -->
