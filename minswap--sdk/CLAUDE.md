# sdk

> - To run a single file in this repo, use `npm run exec <file>`. Example: `npm run exec examples/example.ts`

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/sdk/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

- To run a single file in this repo, use `npm run exec <file>`. Example: `npm run exec examples/example.ts`

---
> Source: [minswap/sdk](https://github.com/minswap/sdk) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-07-21 -->
