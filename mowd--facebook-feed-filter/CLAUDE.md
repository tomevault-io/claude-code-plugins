# facebook-feed-filter

> - Before creating commits in this repository, review recent `git log` entries and write a clear commit message that matches the existing style.

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/facebook-feed-filter/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

# Repository Guidelines

- Before creating commits in this repository, review recent `git log` entries and write a clear commit message that matches the existing style.
- When a version number is bumped in this repository, create the matching git tag using the existing `vX.Y.Z` format.

---
> Source: [Mowd/facebook-feed-filter](https://github.com/Mowd/facebook-feed-filter) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-08-25 -->
