# openresponses

> - Run `bun run prettier --check .` before pushing changes.

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/openresponses/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

# Repository Instructions

- Run `bun run prettier --check .` before pushing changes.
- To fix formatting, run `bun run prettier --write <path>` for targeted files or `bun run prettier --write .` for the full repo.

---
> Source: [openresponses/openresponses](https://github.com/openresponses/openresponses) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-07-21 -->
