# openseadragon

> This project is written in JavaScript, but we also provide TypeScript declarations in types/index.d.ts. In addition, we have a TypeScript test suite in test-dts/index.test-d.ts. When reviewing pull requests, please verify that any changes to the JavaScript code are reflected in the TypeScript declarations and that the TypeScript test suite is updated when appropriate.

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/openseadragon/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

This project is written in JavaScript, but we also provide TypeScript declarations in types/index.d.ts. In addition, we have a TypeScript test suite in test-dts/index.test-d.ts. When reviewing pull requests, please verify that any changes to the JavaScript code are reflected in the TypeScript declarations and that the TypeScript test suite is updated when appropriate.

---
> Source: [openseadragon/openseadragon](https://github.com/openseadragon/openseadragon) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-09-10 -->
