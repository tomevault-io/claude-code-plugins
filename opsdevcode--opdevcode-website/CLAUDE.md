# opdevcode-website

> - Prefer the shebang `#!/usr/bin/env bash` over `#!/bin/bash` and other alternatives

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/opdevcode-website/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:


# When writing bash scripts

- Prefer the shebang `#!/usr/bin/env bash` over `#!/bin/bash` and other alternatives
- Use 2 newlines before function definitions, just like in Python
- Use the `function` keyword, not an implicit definition

---
> Converted and distributed by [TomeVault](https://tomevault.io/claim/opsdevcode) — claim your Tome and manage your conversions.
<!-- tomevault:4.0:claude_md:2026-04-09 -->
