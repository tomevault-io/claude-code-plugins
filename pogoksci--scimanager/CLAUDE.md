# scimanager

> Rule: Always use Windows PowerShell native commands (e.g., `Select-String` instead of `grep`, `Get-ChildItem` instead of `ls` or `find`, `Remove-Item` instead of `rm`). Do not attempt to use Linux-specific command line tools.

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/scimanager/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

Environment: Windows
Rule: Always use Windows PowerShell native commands (e.g., `Select-String` instead of `grep`, `Get-ChildItem` instead of `ls` or `find`, `Remove-Item` instead of `rm`). Do not attempt to use Linux-specific command line tools.

---
> Source: [pogoksci/scimanager](https://github.com/pogoksci/scimanager) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-08-13 -->
