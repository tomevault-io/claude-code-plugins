# mariadb-connector-go

> - NEVER run `git commit`, `git push`, `git add && git commit`, or any git write operation.

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/mariadb-connector-go/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:


## CRITICAL — Git Operations
- NEVER run `git commit`, `git push`, `git add && git commit`, or any git write operation.
- Always let the user decide when and how to commit.
- You may run read-only git commands like `git status`, `git diff`, `git log`, `git remote -v`.

---
> Converted and distributed by [TomeVault](https://tomevault.io/claim/rusher) — claim your Tome and manage your conversions.
<!-- tomevault:4.0:claude_md:2026-04-13 -->
