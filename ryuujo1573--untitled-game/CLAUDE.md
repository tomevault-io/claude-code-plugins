# untitled-game

> - **Package Manager**: Use `bun` exclusively (add, remove, update, install). No `npm`/`yarn`/`pnpm`.

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/untitled-game/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

# Project Rules

## Prefer `bun` as JS runtime and package manager.

- **Package Manager**: Use `bun` exclusively (add, remove, update, install). No `npm`/`yarn`/`pnpm`.
- **Testing**: Use `bun run test` to run vitest.
- **Lint/Format**: Use `bun biome check --write`.

---
> Converted and distributed by [TomeVault](https://tomevault.io/claim/ryuujo1573) — claim your Tome and manage your conversions.
<!-- tomevault:4.0:claude_md:2026-04-09 -->
