# opencheck

> This project uses **bun** as its package manager and runtime, not npm.

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/opencheck/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

This project uses **bun** as its package manager and runtime, not npm.

- Use `bun install` instead of `npm install`.
- Use `bun run` instead of `npm run` or `npx`.
- Use `bun test` or `bun run test` instead of `npm test`.
- Never generate or commit `package-lock.json`. The project uses `bun.lock`.

---
> Converted and distributed by [TomeVault](https://tomevault.io/claim/salfatigroup) — claim your Tome and manage your conversions.
<!-- tomevault:4.0:claude_md:2026-04-14 -->
