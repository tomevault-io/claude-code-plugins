# restrictions

> Use YAML file which exists in root "./config.yaml", do not create new yaml file or create function to create a test YAML file.

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/restrictions/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:


# Restrictions
Use YAML file which exists in root "./config.yaml", do not create new yaml file or create function to create a test YAML file.
Do not change function interface until specifically told to do so. Maintain the function name, arguments, output.

---
> Source: [sidetrip-ai/ici-core](https://github.com/sidetrip-ai/ici-core) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-05-18 -->
