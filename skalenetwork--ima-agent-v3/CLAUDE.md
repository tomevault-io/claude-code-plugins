# ima-agent-v3

> * always activate virtual environment: `source .venv/bin/activate`

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/ima-agent-v3/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

* always activate virtual environment: `source .venv/bin/activate`
* use `uv` to manage dependencies and run commands - `uv sync --all-extras --prerelease=allow` to install all dependencies, `uv run <command>` to run commands in the virtual environment

---
> Source: [skalenetwork/ima-agent-v3](https://github.com/skalenetwork/ima-agent-v3) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-06-01 -->
