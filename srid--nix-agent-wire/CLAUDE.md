# nix-agent-wire

> Everything here is source of truth. Deployed to `~/.config/` (opencode, claude) via home-manager. Edit here, not the deployed copies.

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/nix-agent-wire/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

Everything here is source of truth. Deployed to `~/.config/` (opencode, claude) via home-manager. Edit here, not the deployed copies.

Skill supplementary files (like `haskell/RELUDE.md`) are not auto-included on load — agent reads them on demand via relative links in SKILL.md.

# CI

Run locally: `vira ci -b`

---
> Source: [srid/nix-agent-wire](https://github.com/srid/nix-agent-wire) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-05-04 -->
