# luluseek-game

> - Never commit or push `.env` files (e.g. `.env`, `.env.local`, `.env.production`) to GitHub or git version control.

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/luluseek-game/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

# Repository Guidelines

## Environment & Secrets Safety
- Never commit or push `.env` files (e.g. `.env`, `.env.local`, `.env.production`) to GitHub or git version control.
- Always maintain `.env.example` with non-sensitive template keys as the reference for required environment variables.

---
> Source: [Suhailulu/luluseek-game](https://github.com/Suhailulu/luluseek-game) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-08-07 -->
