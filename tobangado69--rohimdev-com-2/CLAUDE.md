# rohimdev-com-2

> - Use HTML5 semantic elements (`<header>`, `<nav>`, `<main>`, `<section>`, `<article>`, `<footer>`)

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/rohimdev-com-2/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:


# HTML Coding Standards

## Structure Requirements
- Use HTML5 semantic elements (`<header>`, `<nav>`, `<main>`, `<section>`, `<article>`, `<footer>`)
- Include proper DOCTYPE declaration
- Add viewport meta tag for responsive design
- Use proper heading hierarchy (h1 → h2 → h3)

## Tailwind CSS Integration
- Include Tailwind CSS via CDN in the `<head>`
- Use Tailwind utility classes for styling
- Avoid inline styles - use Tailwind classes
- Follow mobile-first responsive design principles

## Accessibility
- Include `alt` attributes for all images
- Use proper ARIA labels where needed
- Ensure keyboard navigation works
- Maintain proper color contrast

## Performance
- Optimize images for web (WebP format preferred)
- Minimize external dependencies
- Use lazy loading for images when appropriate

description:
globs:
alwaysApply: true
---

---
> Converted and distributed by [TomeVault](https://tomevault.io/claim/tobangado69)
> This is a context snippet only. You'll also want the standalone SKILL.md file — [download at TomeVault](https://tomevault.io/claim/tobangado69)
<!-- tomevault:4.0:claude_md:2026-04-08 -->
