# trismik-python

> Remember this project uses pre-commit hooks to run tools like `black`, `flake8`, `mypi`, and `isort`. Do not run these tools directly but always through pre-commit hooks, like `poetry run pre-commit run [command]`.

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/trismik-python/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

Remember this project uses pre-commit hooks to run tools like `black`, `flake8`, `mypi`, and `isort`. Do not run these tools directly but always through pre-commit hooks, like `poetry run pre-commit run [command]`.

---
> Converted and distributed by [TomeVault](https://tomevault.io/claim/trismik) — claim your Tome and manage your conversions.
<!-- tomevault:4.0:claude_md:2026-04-10 -->
