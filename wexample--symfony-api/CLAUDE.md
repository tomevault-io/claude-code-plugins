# symfony-api

> This project is managed by the **wex** script.

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/symfony-api/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

This project is managed by the **wex** script.

**With wex installed:**
- Outside `wex talk`: launch `wex ai::agent/talk` from this directory — best experience (commands via MCP, project preprompt loaded).
- Inside `wex talk` on this app's cwd: standard flow, no extra setup needed.
- Inside `wex talk` on another cwd: invoke wex commands with `--app-path <path>` to target this app.

**Without wex:** read `.wex/knowledge/__summary.md` for static documentation.

**Unsure?** Run `wex hi` — prints `hi!` if installed; otherwise see https://github.com/wexample/wex.

---
> Source: [wexample/symfony-api](https://github.com/wexample/symfony-api) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-08-26 -->
