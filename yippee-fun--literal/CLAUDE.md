# literal

> - Never use `is_a?` because it asks the wrong object. Prefer `===` or `in` or `case`/`when`.

## Usage

Add this to your project's CLAUDE.md to activate this skill:

```
Read and follow the instructions in .claude/skills/literal/SKILL.md
```

Or copy the instructions below directly into your CLAUDE.md:

- Never use `is_a?` because it asks the wrong object. Prefer `===` or `in` or `case`/`when`.
- Run tests with `mise run test`, you can always run all the tests becuase they’re very fast.

---
> Source: [yippee-fun/literal](https://github.com/yippee-fun/literal) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:claude_md:2026-07-22 -->
